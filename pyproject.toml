[project]
name = "fabops-decision-lab"
version = "0.6.0"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.8.0",
    "fastapi>=0.116.0",
    "jsonschema>=4.0",
    "neo4j>=5.28.0",
    "psycopg[binary]>=3.2.0",
    "uvicorn>=0.35.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "httpx>=0.28.0",
    "jsonschema>=4.26.0",
    "pytest>=9.1.1",
    "ruff>=0.12.0",
]

[tool.ruff]
target-version = "py311"
line-length = 140

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]

